import NavMenu from "../NavMenu/NavMenu";

import "./Header.css";

const Header = () => {
  return (
    <header className={"header-root"}>
      <NavMenu />
    </header>
  );
};

export default Header;
